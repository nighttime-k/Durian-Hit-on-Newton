import { LangToggle } from "@/components/infographic/lang-toggle";
import { useLocale } from "@/lib/locale";
export function SiteNav() { const { t } = useLocale(); const links = [{ href: "#physics", label: t.nav.physics }, { href: "#lab", label: t.nav.lab }, { href: "#timeline", label: t.nav.timeline }, { href: "#map", label: t.nav.map }, { href: "#after", label: t.nav.after }]; return <nav className="sticky top-0 z-40 border-b border-line/80 bg-bg/80 backdrop-blur-md" aria-label={t.nav.aria}><div className="mx-auto flex max-w-6xl items-center gap-3 px-5 py-3 sm:px-8"><a href="#top" className="shrink-0 font-display text-sm font-medium text-fg">{t.nav.brand}</a><ul className="ml-auto flex min-w-0 items-center overflow-x-auto">{links.map((link) => <li key={link.href}><a href={link.href} className="inline-flex min-h-11 items-center px-2 text-sm text-muted transition-colors duration-150 hover:text-fg sm:px-2.5">{link.label}</a></li>)}</ul><LangToggle className="shrink-0" /></div></nav>; }
